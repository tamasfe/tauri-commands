# Example Tauri Code Generation

TypeScript code is generated during runtime if the tauri app is compiled with `debug_assertions` in [`main.rs`](src-tauri/src/main.rs) to [`tauri.ts`](src/tauri.ts).

